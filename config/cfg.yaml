env:
  vehicles_count: 20
  initial_vehicle_count: 10
  absolute: True
  normalize: False
  order: sorted
  spawn_probability: 0.3
pure_mpc:
  horizon: 16
  render: False
  ttc_threshold: 2
mpc_rl:
  v0: # reference speed version
    obs_space_dim: 
    action_space_dim:
  v1: # dynamic weights version
    obs_space_dim: 
    action_space_dim:
rl: